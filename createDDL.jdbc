CREATE TABLE ABSTRACTMODEL (ID BIGINT IDENTITY NOT NULL, DTYPE VARCHAR(31), CREATEDAT TIMESTAMP, UPDATEDAT TIMESTAMP, VERSION BIGINT, CITY VARCHAR, COUNTRY VARCHAR, STREET VARCHAR, ZIP VARCHAR, ACTUALCOSTS DOUBLE, BUDGET DOUBLE, EVENTNAME VARCHAR, MAXCAPACITY INTEGER, ADDRESS_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE ABSTRACTMODEL ADD CONSTRAINT FK_ABSTRACTMODEL_ADDRESS_ID FOREIGN KEY (ADDRESS_ID) REFERENCES ABSTRACTMODEL (ID)
