CREATE TABLE ABSTRACTMODEL (ID BIGINT IDENTITY NOT NULL, DTYPE VARCHAR(31), CREATEDAT TIMESTAMP, UPDATEDAT TIMESTAMP, VERSION BIGINT, CITY VARCHAR, COUNTRY VARCHAR, STREET VARCHAR, ZIP VARCHAR, PRIMARY KEY (ID))
